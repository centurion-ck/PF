[project]
name = "cms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "psycopg2-binary",
    "requests",
    "python-dotenv",
]
